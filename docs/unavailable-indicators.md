# المؤشرات المعطّلة

> وُلِّد بـ `scripts/build-unavailable-doc.mts` من `docs/liveness.json`. لا تحرّره بالإيد.

**114** مؤشراً عُطِّل من أصل **359**. المتبقّي في المحرك: **245**.

التنفيذ لم يُحذف. الملفات تحت `packages/engine/src/indicators/unavailable/` وغير مستوردة من `index.ts`، فالاسم وحده هو ما اختفى من الريجستري. أي واحد منها يعود للعمل بمجرد إعادة سطر الاستيراد ووجود البيانات.

## العيّنة التي بُني عليها الحكم

| | |
|---|---|
| الرموز | 183 |
| القطع المتصلة | 405 من 502 نافذة |
| الشموع | 33501 |
| التقييمات لكل مؤشر | 10744 |
| تغطية الساعات | 24/24 |
| أيام مختلفة | 4 |
| فجوات وُجدت | 10 |
| شموع أُهملت (قطع قصيرة) | 10550 |

> أطول امتداد متصل ~3.2 يوم. عدد التقييمات كبير، لكن التغطية الزمنية ليست كذلك — كل الأزواج تُسجَّل في نفس اللحظات.

## المعايير

| # | المعيار |
|---|---|
| 1 | قيمة واحدة في 100% من التقييمات |
| 2 | NaN / undefined / exception ولو مرة |
| 3 | يقرأ مدخلاً غير موجود في البيانات |
| 4 | تنفيذ ثابت مكتوب في الكود |

## المعطَّل، حسب المدرسة

### بيانات خارجية — 37

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `advance_decline` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `advance_decline_line` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `arms_index` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `astro` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `bpi` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `breadth_thrust` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `bullish_percent_index` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `commitment_of_traders` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `cot_report` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `cross_asset_flow_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `dealer_positioning_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `dxy` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `economic_calendar_impact_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `gold_ratio` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `intermarket` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `lunar_cycle` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `macro_cycle_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `market_breadth` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `mcclellan_oscillator` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `mcclellan_summation` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `new_highs_lows` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `news_sentiment_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `nhnl` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `put_call_ratio` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `relative_rotation_graph` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `risk_on_off` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `rrg` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `seasonality` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `sector_rotation` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `social_sentiment_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `tick` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `tick_index` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `trin` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `us10y` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `vix` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `yield_curve` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `zweig_breadth` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |

**شرط العودة:** اشتراك في مزوّد بيانات سوق

### تعلّم آلي — 22

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `autoencoder` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `bayesian_networks` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `clustering_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `cnn_pattern` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `deep_learning_pattern_recognition` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `gaussian_process_regression` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `gradient_boosting` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `hidden_markov_models` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `ica` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `lstm` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `neural_network` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `pca` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `quantitative_factor_models` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `random_forest` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `reinforcement_learning_models` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `som` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `svm_classifier` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `tcn` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `transformer` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `transformer_based_time_series` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `wavenet` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `xgboost` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |

**شرط العودة:** نموذج مدرَّب + نقطة استدلال

### تدفّق أوامر ومشتقات — 21

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `cointegration_spread_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `delta_exposure` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `dom` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `footprint` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `funding_rate_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `gamma_exposure` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `hidden_liquidity_models` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `iceberg_orders` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `kelly_criterion` | نسبة كيلي لحجم المخاطرة من تاريخ نتائج الصفقات | **1** قيمة واحدة (0) في 10744 تقييم | لا يوجد — يحتاج تاريخ حساب لا يصل للمؤشر |
| `latency_arbitrage_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `liquidation_heatmap_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `liquidity_heatmaps` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `long_short_ratio_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `max_pain_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `microstructure_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `options_open_interest_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `risk_parity_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `smart_tape_reading` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `smt_divergence` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `spoofing` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `statistical_arbitrage` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |

**شرط العودة:** دفتر أوامر أو تغذية مشتقات

### حجم — 19

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `cmf` | تدفّق أموال تشايكن — ضغط الشراء مقابل البيع (-1..1) | **3** يقرأ الحجم وهو غير موجود في البيانات | bop — ميزان القوة من موقع الإغلاق داخل الشمعة |
| `cumulative_volume_delta` | دلتا الحجم التراكمية | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `cvd` | دلتا الحجم التراكمية | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `ease_of_movement` | سهولة الحركة | **3** يقرأ الحجم وهو غير موجود في البيانات | atr |
| `elder_force_index` | قوة إلدر — التغيّر السعري × الحجم | **3** يقرأ الحجم وهو غير موجود في البيانات | bull_power / bear_power — نفس مدرسة إلدر بدون حجم |
| `emv` | سهولة الحركة — كم حجم لزم لتحريك السعر | **3** يقرأ الحجم وهو غير موجود في البيانات | atr — مدى الحركة بدون الحجم |
| `klinger` | مذبذب كلينجر — اتجاه الحجم طويل المدى | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `klinger_oscillator` | مذبذب كلينجر | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `liquidity_score` | درجة السيولة عند المستوى الحالي | **3** يقرأ الحجم وهو غير موجود في البيانات | sr_support / sr_resistance |
| `mfi` | مؤشر تدفّق الأموال — RSI مرجّح بالحجم (0-100) | **3** يقرأ الحجم وهو غير موجود في البيانات | rsi — نفس فكرة التشبّع بدون ترجيح |
| `nvi` | مؤشر الحجم السالب — تتبّع «المال الذكي» في أيام الحجم المنخفض | **1** قيمة واحدة (1000) في 10744 تقييم · **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `obv` | الحجم التراكمي حسب اتجاه الإغلاق | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `pvi` | مؤشر الحجم الموجب — تتبّع «مال الجمهور» | **1** قيمة واحدة (1000) في 10744 تقييم · **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `pvt` | الحجم مرجّحاً بنسبة تغيّر السعر | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `vol_delta` | الفرق بين حجم الشراء والبيع | **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `vol_ratio` | نسبة الحجم الحالي للمتوسط | **1** قيمة واحدة (1) في 10744 تقييم · **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `volume` | حجم الشمعة مقارنةً بمتوسطه | **1** قيمة واحدة (1) في 10744 تقييم · **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد — لا بديل سعري عن الحجم |
| `volume_oscillator` | الفرق بين متوسطي حجم سريع وبطيء | **1** قيمة واحدة (0) في 10744 تقييم · **3** يقرأ الحجم وهو غير موجود في البيانات | لا يوجد |
| `volume_profile` | تصنيف العقدة السعرية حسب كثافة الحجم | **3** يقرأ الحجم وهو غير موجود في البيانات | market_profile — نفس الفكرة من التوزيع الزمني |

**شرط العودة:** أن يوفّر مصدر الشموع حجماً حقيقياً

### أون-تشين — 12

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `exchange_netflow` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `exchange_reserves` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `hash_rate` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `mining_difficulty` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `mvrv` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `nupl` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `on_chain_analysis` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `pi_cycle` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `realized_pnl` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `sopr` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `stock_to_flow` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `whale_alert` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |

**شرط العودة:** ربط مفهرس بلوكتشين

### كلاسيكي — 1

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `ad_line` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد |

**شرط العودة:** توفّر البيانات التي يحتاجها

### إحصائي — 1

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `kalman_filter` | — | **1** قيمة واحدة (0) في 10744 تقييم · **4** تنفيذ ثابت مكتوب في الكود | لا يوجد |

**شرط العودة:** توفّر البيانات التي يحتاجها

### وايكوف — 1

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `wyckoff_phase` | مرحلة دورة وايكوف (ps · sc · ar · st · sos · lps · bc · utad) | **3** يقرأ الحجم وهو غير موجود في البيانات | wyckoff_spring · wyckoff_upthrust · accumulation · distribution — أجزاء المرحلة كلٌ على حدة، وكلها سعرية |

**شرط العودة:** توفّر البيانات التي يحتاجها

## مرشَّح — بانتظار داتا أوسع

**14** مؤشراً سقط بالمعيار 1 وحده، ولم يُشَل.

العيّنة تغطي 24/24 ساعة لكن ~3.2 يوم متصلة فقط. هذا يكفي للحكم على مؤشر زخم أو اتجاه، ولا يكفي لنمط يظهر مرة في الشهر ولا لمرشّح جلسات. الحكم عليها مؤجَّل حتى يتراكم تاريخ أطول.

| المؤشر | المدرسة | القيمة الوحيدة المرصودة |
|---|---|---|
| `bear_flag` | كلاسيكي | `none` |
| `bull_flag` | كلاسيكي | `none` |
| `island_reversal` | كلاسيكي | `none` |
| `liquidity` | ICT | `none` |
| `no_demand` | كلاسيكي | `none` |
| `no_supply` | كلاسيكي | `none` |
| `reaccumulation` | وايكوف | `none` |
| `redistribution` | وايكوف | `none` |
| `rounding_bottom` | كلاسيكي | `none` |
| `rounding_top` | كلاسيكي | `none` |
| `silver_bullet` | ICT | `none` |
| `three_drives` | كلاسيكي | `none` |
| `vcp` | كلاسيكي | `none` |
| `vsa` | كلاسيكي | `none` |

## الباقي في المحرك

**245** مؤشراً أنتج قيمتين مختلفتين على الأقل من حساب حقيقي.

`vwap` و `price_vs_vwap` استُثنيا من الشيل رغم قراءتهما للحجم: الحجم الثابت يُختصر من `Σ(typical × V) / ΣV` تماماً فيتبقّى متوسط السعر النموذجي — مُتحقَّق منه حتى 1e-12 في `test/volume-meta.test.ts`. حساب صحيح باسم مضلِّل، لا رقم فاسد.

