# المؤشرات المعطّلة

> وُلِّد بـ `scripts/build-unavailable-doc.mts` من `docs/liveness.json`. لا تحرّره بالإيد.

**130** مؤشراً عُطِّل من أصل **359**. المتبقّي في المحرك: **229**.

التنفيذ لم يُحذف. الملفات تحت `packages/engine/src/indicators/unavailable/` وغير مستوردة من `index.ts`، فالاسم وحده هو ما اختفى من الريجستري. أي واحد منها يعود للعمل بمجرد إعادة سطر الاستيراد ووجود البيانات.

## العيّنة التي بُني عليها الحكم

| | |
|---|---|
| الرموز | 183 |
| القطع المتصلة | 422 من 502 نافذة |
| الشموع | 34116 |
| التقييمات لكل مؤشر | 10756 |
| تغطية الساعات | 24/24 |
| أيام مختلفة | 4 |
| فجوات وُجدت | 26 |
| شموع أُهملت (قطع قصيرة) | 10068 |

> أطول امتداد متصل ~3.2 يوم. عدد التقييمات كبير، لكن التغطية الزمنية ليست كذلك — كل الأزواج تُسجَّل في نفس اللحظات.

## المعايير

| # | المعيار |
|---|---|
| 1 | قيمة واحدة في 100% من التقييمات |
| 2 | NaN / undefined / exception ولو مرة |
| 3 | يقرأ مدخلاً غير موجود في البيانات |
| 4 | تنفيذ ثابت مكتوب في الكود |

## المعطَّل، حسب المدرسة

### بيانات خارجية — 37

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `advance_decline` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `advance_decline_line` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `arms_index` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `astro` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `bpi` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `breadth_thrust` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `bullish_percent_index` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `commitment_of_traders` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `cot_report` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `cross_asset_flow_analysis` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `dealer_positioning_analysis` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `dxy` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `economic_calendar_impact_analysis` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `gold_ratio` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `intermarket` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `lunar_cycle` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `macro_cycle_analysis` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `market_breadth` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `mcclellan_oscillator` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `mcclellan_summation` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `new_highs_lows` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `news_sentiment_analysis` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `nhnl` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `put_call_ratio` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `relative_rotation_graph` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `risk_on_off` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `rrg` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `seasonality` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `sector_rotation` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `social_sentiment_analysis` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `tick` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `tick_index` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `trin` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `us10y` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `vix` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `yield_curve` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |
| `zweig_breadth` | — |  | لا يوجد — يحتاج مزوّد بيانات سوق منفصل |

**شرط العودة:** اشتراك في مزوّد بيانات سوق

### تعلّم آلي — 22

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `autoencoder` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `bayesian_networks` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `clustering_analysis` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `cnn_pattern` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `deep_learning_pattern_recognition` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `gaussian_process_regression` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `gradient_boosting` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `hidden_markov_models` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `ica` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `lstm` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `neural_network` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `pca` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `quantitative_factor_models` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `random_forest` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `reinforcement_learning_models` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `som` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `svm_classifier` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `tcn` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `transformer` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `transformer_based_time_series` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `wavenet` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |
| `xgboost` | — |  | لا يوجد — يحتاج نموذجاً مدرَّباً ونقطة استدلال |

**شرط العودة:** نموذج مدرَّب + نقطة استدلال

### تدفّق أوامر ومشتقات — 21

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `cointegration_spread_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `delta_exposure` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `dom` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `footprint` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `funding_rate_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `gamma_exposure` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `hidden_liquidity_models` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `iceberg_orders` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `kelly_criterion` | نسبة كيلي لحجم المخاطرة من تاريخ نتائج الصفقات |  | لا يوجد — يحتاج تاريخ حساب لا يصل للمؤشر |
| `latency_arbitrage_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `liquidation_heatmap_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `liquidity_heatmaps` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `long_short_ratio_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `max_pain_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `microstructure_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `options_open_interest_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `risk_parity_analysis` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `smart_tape_reading` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `smt_divergence` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `spoofing` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |
| `statistical_arbitrage` | — |  | لا يوجد — يحتاج دفتر أوامر أو بيانات مشتقات |

**شرط العودة:** دفتر أوامر أو تغذية مشتقات

### حجم — 19

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `cmf` | تدفّق أموال تشايكن — ضغط الشراء مقابل البيع (-1..1) |  | bop — ميزان القوة من موقع الإغلاق داخل الشمعة |
| `cumulative_volume_delta` | دلتا الحجم التراكمية |  | لا يوجد |
| `cvd` | دلتا الحجم التراكمية |  | لا يوجد |
| `ease_of_movement` | سهولة الحركة |  | atr |
| `elder_force_index` | قوة إلدر — التغيّر السعري × الحجم |  | bull_power / bear_power — نفس مدرسة إلدر بدون حجم |
| `emv` | سهولة الحركة — كم حجم لزم لتحريك السعر |  | atr — مدى الحركة بدون الحجم |
| `klinger` | مذبذب كلينجر — اتجاه الحجم طويل المدى |  | لا يوجد |
| `klinger_oscillator` | مذبذب كلينجر |  | لا يوجد |
| `liquidity_score` | درجة السيولة عند المستوى الحالي |  | sr_support / sr_resistance |
| `mfi` | مؤشر تدفّق الأموال — RSI مرجّح بالحجم (0-100) |  | rsi — نفس فكرة التشبّع بدون ترجيح |
| `nvi` | مؤشر الحجم السالب — تتبّع «المال الذكي» في أيام الحجم المنخفض |  | لا يوجد |
| `obv` | الحجم التراكمي حسب اتجاه الإغلاق |  | لا يوجد |
| `pvi` | مؤشر الحجم الموجب — تتبّع «مال الجمهور» |  | لا يوجد |
| `pvt` | الحجم مرجّحاً بنسبة تغيّر السعر |  | لا يوجد |
| `vol_delta` | الفرق بين حجم الشراء والبيع |  | لا يوجد |
| `vol_ratio` | نسبة الحجم الحالي للمتوسط |  | لا يوجد |
| `volume` | حجم الشمعة مقارنةً بمتوسطه |  | لا يوجد — لا بديل سعري عن الحجم |
| `volume_oscillator` | الفرق بين متوسطي حجم سريع وبطيء |  | لا يوجد |
| `volume_profile` | تصنيف العقدة السعرية حسب كثافة الحجم |  | market_profile — نفس الفكرة من التوزيع الزمني |

**شرط العودة:** أن يوفّر مصدر الشموع حجماً حقيقياً

### كلاسيكي — 13

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `ad_line` | — |  | لا يوجد |
| `bear_flag` | — |  | لا يوجد |
| `bull_flag` | — |  | لا يوجد |
| `island_reversal` | — |  | لا يوجد |
| `no_demand` | — |  | لا يوجد |
| `no_supply` | — |  | لا يوجد |
| `price_vs_vwap` | — |  | لا يوجد |
| `rounding_bottom` | — |  | لا يوجد |
| `rounding_top` | — |  | لا يوجد |
| `three_drives` | — |  | لا يوجد |
| `vcp` | — |  | لا يوجد |
| `vsa` | — |  | لا يوجد |
| `vwap` | — |  | لا يوجد |

**شرط العودة:** توفّر البيانات التي يحتاجها

### أون-تشين — 12

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `exchange_netflow` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `exchange_reserves` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `hash_rate` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `mining_difficulty` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `mvrv` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `nupl` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `on_chain_analysis` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `pi_cycle` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `realized_pnl` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `sopr` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `stock_to_flow` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |
| `whale_alert` | — |  | لا يوجد — بيانات بلوكتشين، لا علاقة لها بشموع الفوركس |

**شرط العودة:** ربط مفهرس بلوكتشين

### وايكوف — 3

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `reaccumulation` | — |  | لا يوجد |
| `redistribution` | — |  | لا يوجد |
| `wyckoff_phase` | مرحلة دورة وايكوف (ps · sc · ar · st · sos · lps · bc · utad) |  | wyckoff_spring · wyckoff_upthrust · accumulation · distribution — أجزاء المرحلة كلٌ على حدة، وكلها سعرية |

**شرط العودة:** توفّر البيانات التي يحتاجها

### ICT — 2

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `liquidity` | — |  | لا يوجد |
| `silver_bullet` | — |  | لا يوجد |

**شرط العودة:** توفّر البيانات التي يحتاجها

### إحصائي — 1

| المؤشر | يقيس | سبب السقوط | البديل |
|---|---|---|---|
| `kalman_filter` | — |  | لا يوجد |

**شرط العودة:** توفّر البيانات التي يحتاجها

## مرشَّح — بانتظار داتا أوسع

**0** مؤشراً سقط بالمعيار 1 وحده، ولم يُشَل.

العيّنة تغطي 24/24 ساعة لكن ~3.2 يوم متصلة فقط. هذا يكفي للحكم على مؤشر زخم أو اتجاه، ولا يكفي لنمط يظهر مرة في الشهر ولا لمرشّح جلسات. الحكم عليها مؤجَّل حتى يتراكم تاريخ أطول.

| المؤشر | المدرسة | القيمة الوحيدة المرصودة |
|---|---|---|

## الباقي في المحرك

**229** مؤشراً أنتج قيمتين مختلفتين على الأقل من حساب حقيقي.

`vwap` و `price_vs_vwap` استُثنيا من الشيل رغم قراءتهما للحجم: الحجم الثابت يُختصر من `Σ(typical × V) / ΣV` تماماً فيتبقّى متوسط السعر النموذجي — مُتحقَّق منه حتى 1e-12 في `test/volume-meta.test.ts`. حساب صحيح باسم مضلِّل، لا رقم فاسد.

