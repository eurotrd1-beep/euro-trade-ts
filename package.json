{
  "name": "euro-trade",
  "private": true,
  "version": "1.0.0",
  "description": "EURO TRADE — unified TypeScript monorepo (web, admin, mobile, backend).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:reference": "tsx scripts/build-strategy-reference.mts && node -e \"require('fs').copyFileSync('docs/strategy-reference.json','apps/web/public/strategy-reference.json')\"",
    "build:engine-bundle": "node scripts/build-engine-bundle.mjs",
    "verify": "npm --workspace @euro/engine run test && npm --workspace @euro/web run test && npm --workspace @euro/engine run typecheck && npm --workspace @euro/shared run typecheck && npm --workspace @euro/web run typecheck && npm --workspace @euro/web run build:static"
  }
}
